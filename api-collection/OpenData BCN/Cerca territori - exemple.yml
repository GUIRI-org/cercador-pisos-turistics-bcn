info:
  name: Cerca territori - exemple
  type: http
  seq: 4

http:
  method: GET
  url: https://{{baseURL}}/geoBCN/serveis/territori?q=agullers
  params:
    - name: q
      value: agullers
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
