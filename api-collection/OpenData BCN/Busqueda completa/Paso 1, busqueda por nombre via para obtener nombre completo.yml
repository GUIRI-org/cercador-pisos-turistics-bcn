info:
  name: Paso 1, busqueda por nombre via para obtener nombre completo
  type: http
  seq: 1

http:
  method: GET
  url: https://{{baseURL}}/geoBCN/serveis/territori?q=sants
  params:
    - name: q
      value: sants
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
